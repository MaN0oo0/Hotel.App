
@model IEnumerable<ReceptionVM>
@{
    ViewData["Title"] = "Index";
}
<h1 class="badge rounded-pill bg-secondary p-2 mt-2">Welcome To Reception </h1>

<style>
    tr ,th , tbody , thead{
        text-align:center !important;
    }
    
</style>



<a asp-controller="Reception" asp-action="Create" class="btn btn-primary w-100">Create New Employee</a>

<br /> <br />


<form asp-controller="Reception" asp-action="Index">
    <div class="container-fluid">
    <div class="row d-flex align-items-center">
        <div class="col-lg-10 col-md-6 col-sm-8 col-xs-6">
            <input type="text" class="form-control" name="SearchValue" />
        </div>
        
        @*<div class="col-lg-2 col-md-4 ">
            <input type="submit" value="search" class="btn btn-success" />
        </div>*@
        <div class="col-sm-4 col-lg-2 col-md-6 col-xs-6">
       
            <input type="submit" value="search" class="btn btn-success" />
        </div>
    </div>
</div>
</form>

<br /> <br />
<div class="row">
            
     <div class="our-work text-center pt-5 pb-5 ">

<table class="table table-hover table-striped text-center table-bordered font-bold">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Salary</th>
           @* <th>Date-Of-Bith</th>
            <th>Address</th>*@
       
            <th>Details</th>
            <th>Update</th>
            <th>Delete</th>
        </tr>
    </thead>
    
    <tbody>
        @foreach (var item in Model)
        {
           
            <tr>
                <td>
                    @item.Id
                </td>
                <td>
                    @item.Name
                </td>
                <td>
                    @item.Salary
                </td>
                @*<td>
                    @item.DOB.ToShortDateString()
                </td>
                <td>
                    @item.Address
                </td>*@
               @* <td>
                    @item.Department.Name
                </td>*@
                <td>
                    <a asp-controller="Reception" asp-action="Details" asp-route-id="@item.Id" class="btn btn-success">Details</a>
                </td>
                <td>
                    <a asp-controller="Reception" asp-action="Update" asp-route-id="@item.Id" class="btn btn-warning">Update</a>
                </td>
                <td>
                    <a asp-controller="Reception" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>

</table>
<center>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary w-100">Return</a>
</center>
       </div>

</div>
